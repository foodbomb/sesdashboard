# aws_account_id is only required if terraform is running locally. If running through TF cloud it will be provider automatically
#aws_account_id = ""
environment = "$ENVIRONMENT"
namespace = "$NAMESPACE"
region = "ap-southeast-2"
app_name = "ses-dashboard"
# Update version as per build tag
app_version = "96e890f1-local"
